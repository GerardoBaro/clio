import type { Collection } from '../types/index.js';

export class CollectionsService {
  // Placeholder for future dependencies (repositories, validation).
  async listCollections(): Promise<Collection[]> {
    throw new Error('CollectionsService.listCollections is not implemented yet.');
  }

  async createCollection(): Promise<Collection> {
    throw new Error('CollectionsService.createCollection is not implemented yet.');
  }
}
